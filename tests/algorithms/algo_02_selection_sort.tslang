desnote ========================================
desnote TEST: Algorithm - Selection Sort
desnote Tests: Selection sort implementation
desnote Expected: Array sorted in ascending order
desnote ========================================

Deschodt selectionSort(arr -> int[], size -> int) -> void
    aer i in range(0, size - 1):
        eric minIdx = i -> int
        aer j in range(i + 1, size):
            erif (arr[j] < arr[minIdx]):
                minIdx = j
        erif (minIdx != i):
            eric temp = arr[i] -> int
            arr[i] = arr[minIdx]
            arr[minIdx] = temp

Deschodt printArray(arr -> int[], size -> int) -> void
    aer i in range(0, size):
        peric("  [{i}] = {arr[i]}")

Deschodt Eric() -> int
    peric("=== Selection Sort Test ===")
    
    eric arr -> int[6]
    arr[0] = 29
    arr[1] = 10
    arr[2] = 14
    arr[3] = 37
    arr[4] = 13
    arr[5] = 25
    
    peric("Before sorting:")
    printArray(arr, 6)
    
    selectionSort(arr, 6)
    
    peric("")
    peric("After sorting:")
    printArray(arr, 6)
    
    deschodt 0
